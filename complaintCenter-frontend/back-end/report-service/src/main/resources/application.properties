# Server Configuration
server.port=3005
spring.application.name=report-service

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/report_service_db
spring.datasource.username=report_service_user
spring.datasource.password=reportservice123
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true

# Actuator
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always

# Logging
logging.level.com.projeto.reportservice=DEBUG
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
